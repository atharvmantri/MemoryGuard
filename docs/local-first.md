# Local First

MemoryGuard stores project memories locally in a SQLite database under `.memoryguard/`.
The core workflow does not require a cloud service or external LLM API.

Generated context files are derived from approved memories and repo metadata.
Review pending capture candidates before approving them.
