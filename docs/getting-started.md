# Getting Started

```bash
uv sync --dev
pnpm install
memoryguard init
memoryguard remember "This project uses Flask for the backend."
memoryguard sync
memoryguard status
```

If `memoryguard` is not on your PATH, prefix commands with `uv run`.
