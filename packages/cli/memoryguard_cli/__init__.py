# SPDX-License-Identifier: Apache-2.0
"""MemoryGuard OSS CLI (Typer).

Exposes the `memoryguard` command (init, add, ingest, query, show, list,
contradictions, correct, rm, dashboard, mcp, status) against the local engine,
with an optional ``--remote <url>`` mode routing to the REST API.
"""

__version__ = "0.1.0"
