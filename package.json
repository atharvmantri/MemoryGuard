{
  "name": "memoryguard-oss-alpha",
  "version": "0.1.0",
  "private": true,
  "description": "MemoryGuard keeps AI coding-agent context files current and secret-safe.",
  "license": "Apache-2.0",
  "packageManager": "pnpm@9.12.0",
  "engines": {
    "node": ">=18.18.0"
  },
  "scripts": {
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "dev": "turbo run dev"
  },
  "devDependencies": {
    "turbo": "^2.1.0"
  }
}
